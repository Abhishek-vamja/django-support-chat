# support_chat

Lightweight Django support chat app scaffolding.

Install into your project by adding `support_chat` to `INSTALLED_APPS` and configuring Channels/Redis.

This package includes models, Channels consumers, basic HTTP endpoints, and a minimal widget.
